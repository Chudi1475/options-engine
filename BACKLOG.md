# Improvement backlog (adversarially verified findings)

From the 2026-07-08 multi-agent sweep. Work top-down; anything touching
trade/money logic needs Chudi's approval before shipping.

- [x] **Model estimate can unilaterally trip the hard STOP even with a good live quote in hand** (options-engine; impact high, risk high)
  - In step(), stop_trigger = min(eff, est_pct) whenever est_pct exists, so the Black-Scholes estimate can fire the -70% stop even on a cycle where a fresh, comparable real quote (eff) is well above -70%. The estimate is priced with REALIZED volatility from daily closes (scanner.sigma -> realized_vol), not the option's implied vol. For 0DTE, live IV is routinely far above realized vol, so est-now/est-
  - fix: Only apply the est_pct floor when the real quote is missing/stale/non-comparable (i.e. when comparable is False), not on every cycle. When a comparable real quote exists, let eff govern the stop and use est_pct only to tighten if the quote is provably stale. Alternatively price the estimate off implied vol, not realized vol.
  - evidence: positions.py:125-126 (stop_trigger = min(eff, est_pct)); scanner.py:1043-1044 (est from realized-vol BS); scanner.py:223 (sigma = realized_vol)
- [ ] **Chart re-downloads its own bars and recomputes the FVG, so the picture disagrees with the text ticket** (options-engine ICT/FVG chart rendering — charts.py; impact high, risk medium)
  - The text card's FVG/CE/SL/TP come from market_tools computing confirming_fvg on day_bars = TODAY's 5m bars only (market_tools.py:485-490,564-573), stored in r['fvg']['confirming'] and printed by cards.py:149-161. render_fvg is called with no bars (scanner.py:523,721), so it does a fresh yf.download of 2d/5m, tail(80) (~2 sessions, charts.py:69-80), and recomputes conf/ticket on THAT window (charts
  - fix: Pass the scan's bars into render_fvg (it already accepts bars=) and prefer the already-computed r['fvg']['confirming'] and its ticket when present, only recomputing as a fallback. That makes the boxed gap, CE arrow, and the SL/TP tags match the words verbatim.
  - evidence: charts.py:69-80,104-106 vs market_tools.py:485-490,564-573 and cards.py:149-161; caption at scanner.py:526
- [ ] **Draw the order block behind the confirming FVG — the entry retail never marks** (options-engine ICT/FVG chart rendering — charts.py; impact high, risk medium)
  - fvg.py already isolates the displacement candle (mid = i+1) and the 3-candle pattern i,i+1,i+2. The order block is the last opposite-color candle before that impulse (for a bullish/BISI gap: the last down-close candle at/before i). Traders enter from the OB just as much as the CE; showing both gives a refined-entry zone the CE line alone doesn't. It's derivable from bars already loaded.
  - fix: For the CONFIRMING FVG only (keep clutter low), scan left from its i for the last candle whose close<open (bull) / close>open (bear), and draw a small shaded rectangle over that candle's open-close body labeled 'OB'. Expose the OB edges from find_fvgs so both fvg.py grading and the chart share one definition.
  - evidence: fvg.py:107-132 (has i, mid, o/c arrays to derive the OB); charts.py:169-186 (where boxes are drawn) shows only the FVG is boxed
- [ ] **15-min momentum is measured across the overnight gap, so early-session stock reads get the wrong bias and a wrong-direction plan** (options-engine universal symbol reads — market_tools.py `_do_read`/`resolve`/`plan_levels`, momentum bias, FVG conviction; impact high, risk medium)
  - `_do_read` pulls `m5 = yf.download(..., period='2d', interval='5m')` (a two-day CONTINUOUS series) and computes momentum as `price / c.iloc[-4]` where c is the whole 2-day dropna'd close series. For a stock in the first ~30 min of the session there are only a few bars of today, so `c.iloc[-4]` is one of YESTERDAY's late bars. The '15-minute momentum' then equals the overnight gap + a few minutes, 
  - fix: Compute mom15 from the last ET session only. Move the `day_bars` (single-ET-day) computation above the momentum block and derive mom15 from `day_bars['Close']` (or call strategy.momentum_pct(day_bars, _cfg) so the read and scanner share one definition). Require the -4 lookback bar to share the same ET date as the latest bar; otherwise return None. 24h assets (forex/gold/crypto) are unaffected sinc
  - evidence: market_tools.py:437 (period='2d'), 474-478 (mom15 = price / c.iloc[-4]); vs strategy.py:66-73 (scanner momentum on today-only bars)
- [ ] **Backtest reports, StrategyConfig, old_bracket and ALLOWED_SETUPS are loaded once in Service.__init__ and never refreshed for the life of the daemon** (options-engine scanner.py reliability; impact high, risk medium)
  - main() builds one Service (scanner.py:1454) and calls svc.daemon() (scanner.py:1461). daemon() is a while-True (scanner.py:1365) that re-enters run_session() each trading day but never rebuilds Service. So self.cfg=StrategyConfig(), self.backtest_old, self.backtest_new and self.old_bracket (scanner.py:113-120) are frozen for the entire process lifetime (weeks, given railway.json restartPolicy ALWA
  - fix: Add Service.reload_tunables() that re-reads backtest_results.json and backtest_new_rules.json into self.backtest_old/new/old_bracket and rebuilds self.cfg, then call it at the top of run_session() (runs once per trading day because daemon re-enters run_session each morning) and again in reset_day() when the date flips. Because backtest.py runs overnight, this morning's session then automatically u
  - evidence: scanner.py:113-120 (loaded once), scanner.py:848 (ALLOWED_SETUPS constant), scanner.py:865,875 (gate reads frozen stats), scanner.py:1454-1461 + 1365 (Service built once, daemon loops), positions.py:1
- [ ] **No no-redeploy path to apply learned/retuned changes: proposed_change dead-ends and the allow-list/knobs are code, not data** (options-engine scanner.py reliability; impact high, risk medium)
  - learn.py explicitly never auto-changes a rule and surfaces proposed_change only as text to the owner (learn.py:16-18, 296-299). lessons_digest.md IS picked up live (assistant.py:364-367 reads it fresh per reply), but that only sharpens the conversational brain; it never touches gate_stats, detect_setup, ALLOWED_SETUPS or the brackets. So the 'self-improving' loop is one-way for trading: even an ap
  - fix: Move ALLOWED_SETUPS and the tunable StrategyConfig knobs (entry_start/end, mom_bars, watchlist, min_winrate, tp_half/stop/giveback) into a live_params.json on the DATA_DIR volume, loaded (and validated) by reload_tunables(), with the current class constants kept as built-in fallback when the file is missing or fails validation. Add an owner-only /reload command that calls reload_tunables() immedia
  - evidence: learn.py:16-18 and 296-299 (proposed_change is text-only), assistant.py:355,364-367 (digest read live but brain-only), scanner.py:848 (allow-list in code), strategy.py:32-45 (knobs in code)
- [ ] **Right-axis price tags collide and become unreadable when levels cluster (the common case)** (options-engine ICT/FVG chart rendering — charts.py; impact high, risk low)
  - level_tag() draws a filled label box at the exact y of each level with zero vertical de-collision (charts.py:197-217). It is called for CE(gold), SL(red), TP(green) and the current price px(muted) independently. On a real setup these routinely sit within a fraction of a percent of each other — a BUY where price is currently tapping the CE puts the gold 'CE' box and the muted 'px' box at the same y
  - fix: Collect the (y,color,label) tags first, sort by y, then walk them and push any tag closer than ~1.6x the box height off its neighbor (nudge in data units, keep the leader line at the true level). Draw px last and skip/merge it when it is within a hair of CE. Give px a lighter text color for contrast.
  - evidence: charts.py:197-206 (level_tag, no overlap handling), 210-217 (four tags stacked); px fill #5b6472 with text #0b0d11
- [ ] **Session / killzone shading — the single highest-value ICT context, and it's nearly free here** (options-engine ICT/FVG chart rendering — charts.py; impact high, risk low)
  - ICT traders judge a setup largely by WHEN it forms: London KZ (02:00-05:00 ET) and NY AM KZ (08:30-11:00 ET) are where the plays live; a gap formed at lunch is discounted. The chart already carries ET timestamps (df.index tz_convert ET) and uses integer x positions, so mapping each candle's clock time to its x is trivial. Nothing on the chart tells the trader whether the confirming FVG formed in a
  - fix: Precompute each candle's ET time, find contiguous x-runs inside each killzone window, and ax.axvspan(x0-0.5, x1+0.5, color='#3a4a6a', alpha=0.06, zorder=0) behind everything with a tiny 'NY AM' label at the top of the band. Faint bands = near-zero clutter, huge read value. Guard on df.index being datetime so passed-in non-time indices skip it.
  - evidence: charts.py:75-77 (tz_convert ET), 90 (integer x), 262-264 (already strftimes df.index for ticks)
- [ ] **max_tokens=1200 is too low for Fable 5's always-on thinking — replies (and relayed deep answers) truncate or come back empty** (assistant.py — chat brain; impact high, risk low)
  - respond() calls the API with max_tokens=1200 and no `thinking` param. On claude-fable-5 (the configured BOT_BRAIN_MODEL) thinking is ALWAYS on, and max_tokens is a hard cap on thinking+text combined. With BOT_BRAIN_EFFORT defaulting to 'high', adaptive thinking can eat a large slice of 1200 tokens, so a substantive answer either truncates mid-sentence (stop_reason max_tokens, sent as-is) or the mo
  - fix: Raise the chat max_tokens to leave headroom for thinking + a full reply (e.g. 4000–8000; Telegram itself allows ~4096 chars). Separately, when a turn is relaying an ask_deep result, bump the budget higher. Optionally also detect stop_reason=='max_tokens' and continue/notify instead of sending a truncated fragment.
  - evidence: assistant.py:689 (max_tokens=1200, no thinking param); :693 effort 'high'; deep_think max_tokens=12000 at :418; empty-reply deflection at :720-721
- [ ] **Empty final reply throws away an already-computed deep answer** (assistant.py — chat brain; impact high, risk low)
  - Three paths all funnel to the same generic deflection: (a) the tool loop `for _ in range(5)` exhausts while still on stop_reason=='tool_use' (model kept calling tools) so `reply` stays ''; (b) the relay call returns empty text (see max_tokens finding); (c) any empty completion. In all cases the code returns 'I read it but came back empty — try rephrasing?' even though ask_deep may have already ret
  - fix: Capture the raw deep_think string when ask_deep runs (e.g. stash it in a local), and in the `if not reply:` branch return that raw deep answer verbatim instead of the generic message. Also make the final iteration force text: if the last loop pass is still tool_use, do one more call with tools omitted (or tool_choice none) so the model must produce the relay.
  - evidence: assistant.py:688 (range(5)); :707-717 (loop, no post-loop finalize); :720-721 (generic empty fallback); ask_deep result at :591-593
- [ ] **Relayed deep answers over 4096 chars are silently dropped (Telegram limit + ignored send error)** (assistant.py — chat brain; impact high, risk low)
  - telegram.send_to() posts the full text in one sendMessage call with no chunking; Telegram hard-rejects messages >4096 chars with HTTP 400. _dispatch_brain calls `telegram.send_to(chat_id, reply)` and ignores the returned error string. So when the chat brain relays a long deep answer (deep mode explicitly produces detailed answers, and the reply cap allows up to ~1200 tokens ≈ well over 4096 chars 
  - fix: Chunk long replies before sending (split on paragraph/sentence boundaries into <=4096-char parts and send sequentially), either inside telegram.send_to or in _dispatch_brain. At minimum, check send_to's return value and, on failure, retry with a truncated/split message so something reaches the user.
  - evidence: telegram.py:55-60 (send_to, no chunking); scanner.py:503-504 (`if reply: telegram.send_to(...)` return ignored)
- [ ] **No retry on 429/529/5xx — a transient overload drops the entire turn and any computed deep answer** (assistant.py — chat brain; impact high, risk low)
  - respond()'s loop returns 'My brain hit an error: ...' on any non-200, and deep_think returns an error string on non-200 or on a RequestException, with zero retry. Anthropic 429 (rate limit), 529 (overloaded), and 5xx are explicitly retryable. The damaging case: iteration 1 calls ask_deep (deep_think computes a great answer), iteration 2's relay call hits a 529 → respond returns the error and the a
  - fix: Wrap both API calls in a small retry-with-backoff on status 429/500/529 and on connection/timeout errors (2–3 attempts, exponential sleep, honor retry-after). Consider raising deep_think's timeout given Fable 5 xhigh runtimes, or switching to the official SDK which auto-retries these.
  - evidence: assistant.py:703-705 (respond non-200 returns, no retry); :424-433 (deep_think non-200/exception, no retry); :410 effort xhigh; :423 timeout=300
- [ ] **Quiet-day lessons duplicate forever and pollute the digest the brain reads** (options-engine nightly self-review loop; impact high, risk low)
  - _rebuild_digest never de-duplicates bullets, and _deterministic_review appends the SAME no-trade lesson ('On a day with no clean 15-min momentum push... staying flat is the correct call') every single quiet day. Since the digest keeps only the most-recent 20 bullets (DIGEST_KEEP=20) and the bot has stretches of no-trade days (it is deliberately picky), a week or two of quiet days fills the entire 
  - fix: De-dupe in _rebuild_digest by normalized lesson text (lowercased, whitespace-collapsed) keeping the newest occurrence, and skip appending a lesson entry at all on a no-trade day (or cap identical bullets to one). Also switch DIGEST_KEEP to keep by distinct day rather than raw bullet count so a burst of duplicates cannot swallow the window.
  - evidence: learn.py:186-187 (deterministic quiet-day append), learn.py:251-272 (_rebuild_digest, bullets.append at 264-265, no dedup), assistant.py:358-372 (_lessons_block injects it into every reply)
- [ ] **Reviewer is blind to its own prior lessons, so it cannot build on them, dedup them, or catch a repeated mistake** (options-engine nightly self-review loop; impact high, risk low)
  - _day_brief (the text the reviewer model reasons over) contains only today's market + graded calls. It never includes the running digest of what the bot already learned. So every night the model re-derives lessons from scratch: it re-states things it already knows (driving the duplication in the finding above), and it can never say 'this is the third time I stopped out chasing a late SPX call, I ke
  - fix: Prepend the current lessons_digest.md (and the last few sessions' review lines) into _day_brief, with an instruction: 'Here is what you already learned; do NOT repeat an existing lesson, only add genuinely new ones, and explicitly flag when today confirms or violates a prior lesson.' This both raises quality and structurally suppresses duplicates.
  - evidence: learn.py:151-173 (_day_brief has no digest), learn.py:208-229 (synthesize builds prompt from _day_brief only)
- [ ] **watch_tomorrow, the single most time-relevant output, is dropped from the brain** (options-engine nightly self-review loop; impact high, risk low)
  - Each review produces watch_tomorrow ('one short line on what to watch or do differently'). It is texted to the owner and stored in the jsonl entry, but _rebuild_digest only pulls the 'lessons' array, so watch_tomorrow never reaches assistant._lessons_block. The one output explicitly meant to shape TOMORROW's reasoning evaporates overnight; the brain answers the next morning with no idea what its o
  - fix: Pin the latest watch_tomorrow at the top of the rebuilt digest as a dated 'FOR TODAY' line with a one-day expiry (drop it once its session date is older than the last trading day), separate from the durable lessons list.
  - evidence: learn.py:331 (watch_tomorrow saved to entry), learn.py:294-295 (only owner message uses it), learn.py:264 (_rebuild_digest reads only entry['lessons'])
- [ ] **No singleton guard on the Telegram getUpdates consumer; a second instance splits commands and double-alerts** (options-engine scanner.py reliability; impact high, risk low)
  - telegram.get_messages() does a plain getUpdates and swallows any error, returning [] (telegram.py:160-161), so a Telegram 409 Conflict from a second consumer is invisible. The module deliberately supports both a local Task Scheduler run (python scanner.py, scanner.py:16-17) and the cloud daemon (--daemon). If both run against the same TELEGRAM_BOT_TOKEN (or an accidental double Railway deploy), ge
  - fix: Write an instance lease to state.json / a lock file on the volume ({instance_id, pid, heartbeat_ts}); on startup, if a foreign lease has a fresh heartbeat, the newer instance should refuse to send alerts and only warn the owner. At minimum, stop swallowing the 409 in get_messages: detect status 409 specifically and DM the owner 'another instance is polling this bot' instead of returning [] silentl
  - evidence: telegram.py:148-168 (getUpdates swallows all errors), scanner.py:1347 (session poll) and 1385 (night long-poll), scanner.py:16-17 (local mode supported), railway.json:5-6 (restartPolicy ALWAYS)
- [ ] **A throttled vol download at entry permanently disables the model stop-floor for that position's life** (options-engine; impact medium, risk high)
  - If yfinance vol is throttled at entry, sigma==0 -> est==0 -> est_entry stored as 0.0. In monitor_one, est_pct requires pos.est_entry > 0, so it is None for the whole life of that position. If the live option quote later goes stale or non-comparable (0DTE chains routinely go bid-less near the close), the position then has NO stop signal at all (est floor dead, mark not comparable) and can bleed wel
  - fix: Backfill est_entry the first cycle vol becomes available (recompute a model entry baseline from spot_at_signal once sigma>0), or refuse to open until at least one of a real quote or a valid est_entry exists so every tracked position always has a working stop path.
  - evidence: scanner.py:943-945 and scanner.py:979 (est_entry=est if est>0 else 0.0), scanner.py:1048 (est_pct requires pos.est_entry > 0)
- [x] **Multi-day weekly names can stack a fresh position every day with no open-position cap** (options-engine; impact medium, risk high)
  - scan_entries only blocks re-entry via skipped_today (reset each day) and opened_today (positions dated == today). For SPX/SPY 0DTE this is fine, but a TSLA/QCOM weekly opened Monday is not dated 'today' on Tue-Fri, so a new TSLA:put can open every trading day while the earlier ones are still live. On a trending week the book can accumulate several correlated positions in the same name. Each additi
  - fix: In scan_entries also skip a ticker that already has any non-closed position (self.book has an open position for that ticker), or add a max-concurrent-per-ticker guard, so weeklies do not stack day over day.
  - evidence: scanner.py:886-888 (opened = opened_today; guards same-day only), positions.py:282-283 (opened_today filters p.date == today), scanner.py:962 (correlated only reduces size, no count cap)
- [ ] **Weekly NEW-vs-OLD scoreboard sums over different trade sets, biasing the 'winner'** (options-engine; impact medium, risk medium)
  - weekly_report computes new_total = sum(final_pnl_pct for all week trades) but old_total = sum(old_rules['exit_pct'] for `both`), where `both` is only trades whose old-rules shadow actually closed (exit_pct not None). It then reports diff = new_total - old_total and declares 'NEW/OLD wins by N points'. Summing NEW over all trades but OLD over a subset makes the comparison apples-to-oranges: any wee
  - fix: Restrict new_total to the same `both` set (trades where the old shadow closed) before differencing, or exclude unmatched trades from both sides so the comparison is over one common set.
  - evidence: scoreboard.py:138 (new_total over all week), scoreboard.py:143-149 (old_total over `both`, then diff/winner)
- [ ] **The 70% win-rate gate is measured on a different exit regime than the bot actually trades** (options-engine; impact medium, risk medium)
  - gate_stats enforces MIN_WINRATE (70%) using backtest_old win_rate. backtest_results.json is generated in backtest.py with take_half_pct=None and a single-leg +target/-stop bracket chosen by best_bracket, i.e. a full-position take-profit/stop system. The live bot trades sell-half-at-+25% / give-back-40 / stop-70 (config.py), a materially different exit regime with a different win rate. So the 70% f
  - fix: Gate the win-rate floor on the new-rules backtest (or live stats) that match the traded exits, falling back to old-rules only with an explicit label. Delete or clearly deprecate the unused StrategyConfig exit fields so no future code reads 60/120/-30 by accident.
  - evidence: scanner.py:865-874 (win_rate from backtest_old vs MIN_WINRATE); backtest.py:311-313 (take_half_pct=None, single-leg bracket generates backtest_results.json); strategy.py:43-46 (dead 60/120/-30/10 defa
- [ ] **Momentum noise gate compares a 15-minute displacement to a single 5-minute bar's range, so it under-filters chop** (options-engine universal symbol reads — market_tools.py `_do_read`/`resolve`/`plan_levels`, momentum bias, FVG conviction; impact medium, risk medium)
  - `mom_min = max(MOM_FLOOR_PCT, MOM_ATR_MULT * (atr/price*100))` with MOM_ATR_MULT=1.0. `atr` is the per-5m-bar ATR, but `mom15` is a 3-bar (15-min) move. A random 15-minute displacement scales ~sqrt(3) (~1.73x) of one bar's range, so a pure-noise tape clears a 1-bar-ATR threshold routinely. The comment claims this keeps a quiet tape as chop, but the scaling mismatch means bias fires bullish/bearish
  - fix: Scale the threshold to the momentum window: `mom_min = max(MOM_FLOOR_PCT, MOM_ATR_MULT * sqrt(_cfg.mom_bars) * (atr/price*100))` (sqrt(3) for a random-walk floor) or `* _cfg.mom_bars` for a stricter trend gate. Optionally expose MOM_ATR_MULT via config for tuning against the backtest.
  - evidence: market_tools.py:327 (MOM_ATR_MULT=1.0), 520 (mom_min uses per-bar atr), 519-524 (bias assignment)
- [ ] **prior_close / day_move_pct can silently span two sessions when yfinance hasn't posted today's daily bar** (options-engine universal symbol reads — market_tools.py `_do_read`/`resolve`/`plan_levels`, momentum bias, FVG conviction; impact medium, risk medium)
  - `prior_close = closes_d.iloc[-2]` assumes the last DAILY row is today. During RTH yfinance usually includes a partial current-day daily candle, so iloc[-2] is yesterday — correct. But pre-open and early in the session (and on some 24h symbols where the daily bar posts on a different clock) the current-day daily row is absent, so iloc[-1] is yesterday and iloc[-2] is two days ago. `day_move_pct = p
  - fix: Derive prior_close as the last daily close whose index date is strictly earlier than the latest intraday bar's ET date, e.g. `prior = closes_d[closes_d.index.date < asof_date]; prior_close = prior.iloc[-1] if len(prior) else None`. Guarantees price and prior_close are different sessions.
  - evidence: market_tools.py:454 (prior_close = closes_d.iloc[-2]), 589 (day_move_pct = price/prior_close-1)
- [ ] **FVG premium/discount and the 'liquidity target' are built from one intraday window, then presented as authoritative ICT dealing-range levels** (options-engine universal symbol reads — market_tools.py `_do_read`/`resolve`/`plan_levels`, momentum bias, FVG conviction; impact medium, risk medium)
  - fvg._dealing_range uses max(High)/min(Low) of the passed bars — here a single ET session of 5m bars (`day_bars`). pd_zone (premium/discount) is computed against that session's 50% equilibrium, and confirming_fvg sets the ticket `target_liquidity` to that same session's hi_r/lo_r. Early in a session the range is a handful of bars, so 'in discount / in premium' and the 'next liquidity pool' are near
  - fix: Feed the dealing range from a higher-timeframe/longer window (e.g. the 1mo daily d1 hi/lo, or a fixed lookback) rather than the intraday slice; require a minimum bar count before trusting pd_zone. In confirming_fvg, drop or down-rank tickets whose rr < 1 (or clamp conviction to 'medium' when the FVG target sits inside current structure). Soften the disclaimer to hedge premium/discount when the ran
  - evidence: fvg.py:47-52 (_dealing_range on passed bars), 233-234 (target = hi_r/lo_r), 244 (rr emitted even if <1); market_tools.py:564-573 (fbars=day_bars), 600-617 (disclaimer instructs quoting these)
- [x] **half_card promises a 'MOMENTUM FLIPPED' alert that the give-back exit system never sends** (options-engine; impact medium, risk low)
  - After the +25% half sells, half_card tells the user: 'Next: I'll text MOMENTUM FLIPPED when the move dies - sell the rest then.' The runner exit was changed to a give-back trail; the actual runner alert is trail_card ('the runner gave back enough off its peak'). No 'momentum flipped' text is ever produced. The user is told to wait for a named signal that never arrives, and may hold the runner expe
  - fix: Rewrite the half_card 'Next:' line to describe the give-back trail (e.g. 'I'll text you to sell the rest when it gives back from its peak'), matching trail_card and the entry_card exit plan.
  - evidence: cards.py:377 (half_card 'MOMENTUM FLIPPED' copy) vs cards.py:382-392 (trail_card give-back copy)
- [ ] **Info panel (top-right, inside axes) overlaps the right-edge price tags for any high level** (options-engine ICT/FVG chart rendering — charts.py; impact medium, risk low)
  - The info panel is anchored at axes-fraction (0.987, 0.97), ha=right, so it fills the top-right corner inside the plot (charts.py:243-247). The level tags are annotated at data-x = right (~len+1), which is near the right edge inside the same axes, offset only +7pt (charts.py:202-206). When a level sits near the top of the range — a TP at the buy-side liquidity high is the whole point of the setup —
  - fix: Either move the info panel to the top-LEFT (candles there are usually older/lower-interest) or reserve a right-margin gutter (extend xlim further right and place tags in the gutter past the panel), and drop the panel's y anchor to 0.90 so a top TP tag clears it.
  - evidence: charts.py:243-247 (panel at 0.987,0.97 ha=right) vs 202-206 (tags at x=right, +7pt)
- [ ] **One-line PD-array context chip: 'discount · bullish bias · NY AM KZ'** (options-engine ICT/FVG chart rendering — charts.py; impact medium, risk low)
  - All three facts an ICT trader checks first — is price in premium/discount, does the gap align with bias, and is it a killzone — already exist (pd_zone in fvg.py, r['bias'], and the timestamps). Today they're scattered (equilibrium line, faint P/D implied by position, no time context). A single chip states the read at a glance.
  - fix: Add one line under the title, e.g. 'price in DISCOUNT · bias BULLISH · NY AM killzone' built from conf['pd_zone'], r.get('bias'), and the killzone lookup. Color the zone word green/red. Replaces eye-scanning with one sentence; ~zero added clutter since it reuses the title band.
  - evidence: fvg.py:159 pd_zone, charts.py:103 bias, 251-256 title band has room
- [ ] **Mark the displacement leg on the confirming FVG (disp strength is computed but never shown)** (options-engine ICT/FVG chart rendering — charts.py; impact medium, risk low)
  - find_fvgs computes disp = body/ATR and a strong flag (fvg.py:124-130), the literal justification for the grade, but the chart never surfaces which candle is the displacement or how strong it was. Traders trust a gap far more when they can see a clean 1.8x-ATR impulse candle created it.
  - fix: On the confirming FVG's mid candle, thicken the candle outline or add a small '⚡1.8x' tag reading conf['disp']. Only on the confirming one, so it stays clean.
  - evidence: fvg.py:124-130,186 (disp/strong in the dict), charts.py:188-194 (CE arrow drawn but no displacement mark)
- [ ] **Universal reads always use ~15-min-delayed yfinance even when Alpaca real-time is configured** (options-engine universal symbol reads — market_tools.py `_do_read`/`resolve`/`plan_levels`, momentum bias, FVG conviction; impact medium, risk low)
  - `_do_read` calls `yf.download` directly for every symbol, so `read_any('nvda')` never touches the DataFeed/Alpaca path the live scanner uses. When ALPACA_API_KEY/SECRET are set, plain stocks have genuinely real-time IEX bars available, but the read hard-labels 'yfinance, ~15-min delayed' and quotes a stale-r price/level than the scanner would. The two surfaces of the same bot disagree on price for
  - fix: Route the intraday pull for stock-kind symbols through DataFeed (today_bars/latest_price) so reads inherit Alpaca when available, and set the `source` label from `_feed.backend_for(yfs)`. Keep yfinance for indexes and the 24h macro/crypto symbols Alpaca can't serve.
  - evidence: market_tools.py:434-437 (yf.download in _do_read), 628 (source hard-coded '~15-min delayed'); data_feed.py:105-128 (DataFeed already provides real-time for non-'^' symbols)
- [ ] **ask_deep escalates with only the raw question — no conversation history, live bot state, or learned lessons** (assistant.py — chat brain; impact medium, risk low)
  - _run_tool calls deep_think(args.get('question','')) and never passes the `context` parameter deep_think already accepts. The deep brain therefore reasons blind: it doesn't get the recent chat turns, the LIVE BOT STATE block (current ET date/time, who the user is), or the lessons_digest playbook that every normal chat reply is given. A follow-up like 'what about the second one?' escalated to deep l
  - fix: Build a context string from the last few history turns + the same LIVE BOT STATE block respond() already assembles, and pass it as deep_think's context arg. Also inject _lessons_block() into DEEP_SYSTEM (or the context) so the deep answer uses the learned playbook. This is the single biggest 'make it noticeably smarter' change.
  - evidence: assistant.py:591-593 (ask_deep passes only question); deep_think signature :401 (unused context=''); DEEP_SYSTEM :382-398 has no lessons; _lessons_block :358
- [ ] **No stop_reason=='refusal' handling or fallback for Fable 5 — a classifier decline becomes an unhelpful deflection** (assistant.py — chat brain; impact medium, risk low)
  - On claude-fable-5 a safety-classifier decline returns HTTP 200 with stop_reason=='refusal' and empty (or partial) content — not an error. respond() and deep_think both only branch on 'tool_use' vs. join text, so a refusal produces empty text and the user gets 'I read it but came back empty' / 'the deep brain came back empty', which reads as a deflection (the exact behavior the system prompt forbid
  - fix: Check `body.get('stop_reason') == 'refusal'` and either add the server-side `fallbacks` param (betas=['server-side-fallback-2026-06-01'], fallbacks=[{'model':'claude-opus-4-8'}]) so the request auto-reruns on Opus, or on refusal re-send once to BOT_DEEP_MODEL/opus. At minimum return an honest, non-deflecting note rather than 'came back empty'.
  - evidence: assistant.py:707-717 (respond only handles tool_use/text); :424-431 (deep_think same); model is claude-fable-5 per .env BOT_BRAIN_MODEL
- [ ] **Nightly review runs on the everyday model, not the deep brain, wasting the best chance to compound** (options-engine nightly self-review loop; impact medium, risk low)
  - synthesize calls assistant.complete, which uses model() = BOT_BRAIN_MODEL (default claude-sonnet-4-6) at 'high' effort with no extended thinking. The nightly review is the highest-leverage reasoning task in the system (its output silently steers every future reply) and it runs exactly once per day, so cost is negligible. deep_think already wires up the strongest brain (BOT_DEEP_MODEL) with adaptiv
  - fix: Add a deep variant of assistant.complete (deep_model + thinking:{type:'adaptive'} + higher max_tokens) and call it from synthesize, falling back to the current complete() then _deterministic_review. One extended-thinking call per night directly improves the lessons that compound.
  - evidence: learn.py:212 (assistant.complete), assistant.py:326-352 (model() default sonnet, complete() no thinking), assistant.py:401-433 (deep_think shows the stronger path exists)
- [ ] **Friday-night review is lost forever if the bot is offline during the 21:00-23:45 window** (options-engine nightly self-review loop; impact medium, risk low)
  - maybe_learn returns immediately on now.weekday() >= 5, and there is no weekend catch-up. If the daemon is down during Friday's random 21:00-23:45 fire window and comes back Saturday, Friday's session is never graded (learn_sent[Friday] was never set, and Saturday/Sunday are skipped). This is an asymmetry: maybe_recap and maybe_weekly both have explicit weekend catch-up ('or now.weekday() >= 5'). A
  - fix: Give maybe_learn the same weekend catch-up as recap/weekly: on Sat/Sun, if learn_sent for the most-recent prior weekday is unset, run learn.run(require_date=that Friday). Keep the once-per-session dedup key so it still fires only once.
  - evidence: scanner.py:1265 (maybe_learn: 'now.weekday() >= 5' -> return), scanner.py:1296 (maybe_weekly has 'or now.weekday() >= 5' catch-up)
- [ ] **No idempotency: re-running a session double-writes its lessons and lets an old backfill masquerade as the newest guidance** (options-engine nightly self-review loop; impact medium, risk low)
  - run() unconditionally _append_lesson()s a new entry every call, keyed by nothing. Running 'learn.py --date 2026-06-01' (backfill/testing) or any accidental re-fire appends a second entry for that session, so the digest counts its bullets twice. Worse, _rebuild_digest orders bullets by append position (bullets[-DIGEST_KEEP:][::-1]), not by session date, so a backfilled OLD day lands at the end of t
  - fix: Make _append_lesson an upsert by session (replace any existing entry with the same 'session' before writing), and sort the digest by actual session date descending instead of by file append order.
  - evidence: learn.py:234-236 (_append_lesson append-only), learn.py:343 (unconditional append in run), learn.py:266 (bullets[-DIGEST_KEEP:][::-1] orders by append position)
- [ ] **Dead STALE guard and no 'did a session actually run' check misattributes outages as clean no-trade days** (options-engine nightly self-review loop; impact medium, risk low)
  - maybe_learn checks 'if errs == "STALE"' but learn.run never returns STALE (only [] or [err]) — confirmed by grep, the branch is unreachable. It was copied from recap, which DOES return STALE, but learn was never given the equivalent guard. Consequence: if the trading day never really happened (data feed down all day, daemon crash-looping, zero positions opened), grade_day returns trades=[] and the
  - fix: Delete the dead STALE branch, and before grading assert the session was real: require either at least one position for the date OR a healthy market-context fetch OR a recorded recap_sent for that day; otherwise skip (do not mark learn_sent) so it retries / stays silent instead of inventing a clean-day lesson.
  - evidence: scanner.py:1273-1275 (unreachable 'errs == STALE'), learn.py:341/348/352/354 (run only returns []/[err]), learn.py:182-187 (fabricates the 'stayed out' narrative on empty trades)
- [ ] **Propose-only guardrail has no dedup or tracking, and rule changes can leak in as plain lessons** (options-engine nightly self-review loop; impact medium, risk low)
  - proposed_change is shown to the owner and buried in the jsonl, but nothing tracks whether it was approved/rejected, so the SAME rule change gets re-proposed night after night (alert fatigue, and no single place shows what is pending). Separately, the guardrail assumes rule changes only ever appear in proposed_change, but nothing stops the model from phrasing a threshold change AS a lesson (e.g. 'b
  - fix: Persist proposals in state.json with a status (pending/approved/rejected/superseded), suppress re-proposing an identical pending one, and add a /proposals owner view. Add a light sanitizer that scans each lesson bullet for imperative rule/threshold language (percent thresholds, 'stop', 'window', 'win rate', 'take half', 'allow') and routes those into proposed_change instead of the digest.
  - evidence: learn.py:322-333 (proposed_change stored, never tracked), learn.py:296-299 (_owner_message re-emits every night), learn.py:222-223 (lessons written verbatim into digest with no rule-change filter)
- [ ] **Once-per-day jobs send BEFORE writing their dedup key, so a crash/redeploy between send and mark re-broadcasts the whole report** (options-engine scanner.py reliability; impact medium, risk low)
  - Every once-per-day job sends first and sets the state dedup key only after: recap (scanner.py:1211-1219), weekly (scanner.py:1302-1305 notify then state_set weekly_sent), morning (scanner.py:419-421 notify then state_set morning_sent), learn (learn.py:343-349 append+rebuild+send, then scanner.py:1277 sets learn_sent). Railway redeploys are frequent and restartPolicy is ALWAYS, so a restart landing
  - fix: Make the dedup write an unconditional attempt record that is incremented BEFORE the send, not only on error, and cap total attempts (send-count, not just error-count). Keep 'duplicate beats missing' as the tie-breaker, but a job that has already been attempted N times today should not re-broadcast on restart. For learn specifically, key _append_lesson/_rebuild_digest by session date and de-dup so 
  - evidence: scanner.py:1211-1219 (recap), scanner.py:1302-1305 (weekly), scanner.py:419-421 (morning), learn.py:343-349 + scanner.py:1277 (learn), scanner.py:1224-1231 (retry only on error)
- [ ] **A nightly self-review missed while the bot is down is silently lost forever (no catch-up), unlike recap/weekly** (options-engine scanner.py reliability; impact medium, risk low)
  - recap and weekly have explicit catch-up in the daemon night branch (scanner.py:1377-1380 recap catch-up, weekly weekend catch-up), but maybe_learn only ever runs for now.date() and guards on learn_sent==today (scanner.py:1265-1272). If the bot is down across the entire 21:00-23:45 window (e.g. a long redeploy or crash), learn_sent never equals that date and the code never revisits it: yesterday's 
  - fix: Give learn the same catch-up recap/weekly have: if learn_sent < the most recent completed weekday session and that session has tracked positions, run it once on the next daemon night tick (bounded so a long outage doesn't back-fill many nights of DMs, e.g. only the single most recent missed session). Optionally persist skipped_today per-date in state.json so a mid-window restart doesn't re-do earn
  - evidence: scanner.py:1265-1272 (learn only for today, no catch-up), scanner.py:1377-1380 (recap/weekly DO catch up), scanner.py:121-133 (in-memory-only guards)
- [ ] **Scorekeeping counts a $0 breakeven trade as a loss** (assistant.py — chat brain; impact low, risk medium)
  - score() defines losses = entries with profit_dollars <= 0, so a scratched/breakeven trade logged as $0 is counted as a loss and drags win_rate_pct down. For a bot whose whole scorekeeping pitch is an honest running record, a $0 flat is not a loss.
  - fix: Treat >0 as win, <0 as loss, and either exclude ==0 from both (a 'scratch' bucket) or at least don't count it as a loss in win_rate. Report scratches separately if you want completeness.
  - evidence: assistant.py:498-504 (losses = profit_dollars <= 0; win_rate uses wins/entries)
- [ ] **Degenerate flat-range chart: equilibrium, BSL and SSL collapse onto one line with stacked labels** (options-engine ICT/FVG chart rendering — charts.py; impact low, risk low)
  - On a dead-flat or near-flat window (illiquid ticker, halted, or a single repeated print) hi_r == lo_r so span=0 and eq==hi_r==lo_r (charts.py:107-108). The equilibrium line, the BSL line and the SSL line are then drawn at the same y with three labels ('equilibrium','BSL liquidity','SSL liquidity') stacked on top of each other (charts.py:153-164), and _pd_zone returns 'equilibrium' for everything (
  - fix: When span <= a tiny epsilon (e.g. < 1e-6*price or < a fraction of ATR), skip the equilibrium/BSL/SSL trio and the FVG detection, and set an explicit y-window around price so the candles still render. Optionally stamp 'flat range — no dealing range' so it reads as intentional.
  - evidence: charts.py:107-108 (eq from hi_r/lo_r), 153-164 (three lines/labels), fvg.py:55-64 (_pd_zone span<=0)
- [ ] **Passed-in bars with a non-datetime index silently drops the whole chart** (options-engine ICT/FVG chart rendering — charts.py; impact low, risk low)
  - The x tick labels do df.index[i].strftime('%H:%M') (charts.py:264). render_fvg accepts a bars= DataFrame (charts.py:37,66) but if a caller ever passes bars whose index isn't a DatetimeIndex (RangeIndex, a backtest frame), strftime raises, the whole try aborts, and render returns (None, reason) — the trader gets no chart with a cryptic reason instead of a chart with plain integer/'#' labels. Latent
  - fix: Guard the tick labels: if getattr(df.index, 'inferred_type', '') isn't datetime (or the element lacks strftime), fall back to bar-index labels. Pairs with passing the scan's bars in.
  - evidence: charts.py:264 (df.index[i].strftime) with bars= accepted at 37,66
- [ ] **resolve() returns a dead yfinance symbol for index aliases and lets crypto aliases shadow same-named stocks** (options-engine universal symbol reads — market_tools.py `_do_read`/`resolve`/`plan_levels`, momentum bias, FVG conviction; impact low, risk low)
  - resolve('spx') falls through to the generic ticker branch and returns ('SPX','SPX',2,'stock'); yf.download('SPX') is not the S&P 500 (that's ^GSPC) and yields 'no recent data', so a plain index read fails even though the bot clearly supports SPX elsewhere. Same for 'ndx'/'vix'. Separately, single-word crypto aliases (sol, link, near, ton, dot, trx) unconditionally resolve to <SYM>-USD, so a user a
  - fix: Add an index alias map (spx->^GSPC, ndx->^NDX/QQQ, vix->^VIX, dji->^DJI) checked before the generic ticker branch, with kind 'index'. For crypto aliases, prefer an explicit 'x' or '-usd' disambiguation, or check the curated stock/list first so a real ticker wins over a coin alias where they collide.
  - evidence: market_tools.py:298-318 (resolve fall-through), 285-293 (CRYPTO alias map), 316-317 (_TICKER_RE returns any 1-6 char upper as a 'stock')
- [ ] **Text/CSV files are silently truncated at 20000 chars with no notice to the model** (assistant.py — chat brain; impact low, risk low)
  - _file_blocks slices file bodies to MAX_TEXT_FILE (20000 chars) but wraps them as '[Contents of the file ...]' with no indication the content was cut. The model can then confidently answer about 'the whole file' (totals, last rows, summary) while only seeing the head, producing wrong answers on larger CSV/log exports the traders paste in.
  - fix: When len(body) hits the cap, append an explicit marker like '[truncated: showing first 20000 of N chars]' so the model knows it saw a partial file and can say so. Optionally raise the cap given Fable 5's 1M context.
  - evidence: assistant.py:32 (MAX_TEXT_FILE=20000); :641-645 (truncate then wrap with no truncation notice)
- [ ] **Deterministic fallback tells the bot to 'bank half earlier' on trades where half was already banked** (options-engine nightly self-review loop; impact low, risk low)
  - The stop-out lesson fires on (not won) and exit_reason=='stop' and mfe>=10, with no check of banked_half. But a position can hit the stop AFTER banking half at +25% (weighted_final of a +25 half and a -70 remainder is a net loss that is still exit_reason 'stop'). In that case the lesson 'when a call spikes double digits early, bank half into the spike instead of waiting' is factually wrong (we did
  - fix: Add 'and not o.get("banked_half")' to the condition; when half WAS banked and it still stopped, draw the correct lesson (the runner give-back/stop management, not the half-target).
  - evidence: learn.py:192-198 (stop lesson condition ignores banked_half), positions.py:125-130 (stop path runs after half_sold, weighted_final still labels it 'stop')
- [ ] **Night grading can grade stragglers as STILL OPEN and count trades inconsistently** (options-engine nightly self-review loop; impact low, risk low)
  - _grade_positions grades whatever state each position is in and never force-settles. If the bot was down at the 16:00 settle, a 0DTE position stays state=='open' at review time; position_story returns 'STILL OPEN', won/closed are both False, yet it is still counted in len(trades) and printed in the owner digest as an ungraded call. force_expire only runs via needs_monitoring in the daytime session,
  - fix: In _grade_positions, force-settle any still-open, past-or-at-expiry position for the session (reuse PositionBook._force_expire / needs_monitoring semantics) before grading, so the night review sees the same settled truth the next session would.
  - evidence: learn.py:52-85 (_grade_positions grades raw state, no settle), positions.py:253-276 (_force_expire only reached via needs_monitoring), recap.py:96-100 (position_story STILL-OPEN branch)
- [ ] **Paper-mode trades shape live lessons with no distinction** (options-engine nightly self-review loop; impact low, risk low)
  - grade_day pulls every position for the date via book.for_date, including paper==True practice signals, and treats them identically to real tracked signals when synthesizing lessons that go into the brain's permanent digest. A run of paper experiments could skew the 'what my winners/losers had in common' read that then influences real replies.
  - fix: Either exclude paper positions from the lesson synthesis, or tag them in _day_brief ('[PAPER]') so the reviewer weights them appropriately and does not generalize practice outcomes into live guidance.
  - evidence: learn.py:52-85 (_grade_positions iterates book.for_date with no paper filter), positions.py:55 (paper flag exists), recap.py:254 (recap already distinguishes '[PAPER]')
- [ ] **Day-rollover and once-per-day dedup design is largely sound; concentrate effort on the reload/singleton items above** (options-engine scanner.py reliability; impact low, risk low)
  - Worth stating so effort isn't spent here: the learn fire-time is seeded off the date (learn_target, scanner.py:65-73) so restarts re-derive the same minute instead of double-firing; the window is deliberately kept before midnight ET (scanner.py:56-58) so same-day dedup never rolls over; reset_day clears per-day state on the date flip (scanner.py:195-199); the transient dedup dicts self-prune to to
  - fix: No change needed to the rollover/dedup mechanics themselves; invest in reload_tunables(), the live_params.json data-driven allow-list/knobs, and an instance singleton guard.
  - evidence: scanner.py:65-73 (seeded learn time), scanner.py:56-58 (pre-midnight window), scanner.py:195-199 (reset_day), scanner.py:269-274 (self-pruning flags), telegram.py:185-191 (replay-safe offset)
